export function getStorageOptions(storage) {
    const useIndexedDB = !storage ||
        (Array.isArray(storage) && storage.includes("indexedDB")) ||
        storage === "indexedDB";
    return { useIndexedDB };
}
//# sourceMappingURL=storageOptions.js.map