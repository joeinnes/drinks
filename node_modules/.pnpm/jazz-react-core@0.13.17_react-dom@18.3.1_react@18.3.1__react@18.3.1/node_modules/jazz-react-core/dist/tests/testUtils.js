import { jsx as _jsx } from "react/jsx-runtime";
import { render, renderHook, } from "@testing-library/react";
import { JazzTestProvider } from "../testing.js";
const customRender = (ui, options = {}) => {
    const AllTheProviders = ({ children, }) => {
        return (_jsx(JazzTestProvider, { account: options.account, isAuthenticated: options.isAuthenticated, children: children }));
    };
    return render(ui, { wrapper: AllTheProviders, ...options });
};
const customRenderHook = (callback, options = {}) => {
    const AllTheProviders = ({ children, }) => {
        return (_jsx(JazzTestProvider, { account: options.account, isAuthenticated: options.isAuthenticated, children: children }));
    };
    return renderHook(callback, { wrapper: AllTheProviders, ...options });
};
// re-export everything
export * from "@testing-library/react";
// override render method
export { customRender as render };
export { customRenderHook as renderHook };
//# sourceMappingURL=testUtils.js.map