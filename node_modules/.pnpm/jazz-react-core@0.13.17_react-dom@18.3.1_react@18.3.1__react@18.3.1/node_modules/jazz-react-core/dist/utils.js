export function getCurrentAccountFromContextManager(contextManager) {
    const context = contextManager.getCurrentValue();
    if (!context) {
        throw new Error("No context found");
    }
    return "me" in context ? context.me : context.guest;
}
export function subscribeToContextManager(contextManager, callback) {
    let unsub = () => { };
    const handler = () => {
        unsub();
        unsub = callback();
    };
    handler();
    return contextManager.subscribe(handler);
}
//# sourceMappingURL=utils.js.map